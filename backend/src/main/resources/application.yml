server:
  port: 8080

spring:
  application:
    name: trafficflow

simulation:
  # Parallel plan-phase worker pool size. 24 workers + 6 named service threads = ~30 total.
  worker-threads: 24
  # Ticks per second (Hz). 30 Hz x 70 vehicles = 2100+ vehicle-updates/sec.
  tick-hz: 30
  # Target number of concurrent vehicles (density default). 70+ is supported via the control
  # panel; the default is tuned so traffic flows smoothly out of the box.
  target-vehicles: 55
  # Approach length in meters (from approach edge to stop line).
  approach-length-m: 120.0
  # Lane width in meters.
  lane-width-m: 3.5
  # Half-width of the central intersection box in meters.
  intersection-half-m: 14.0
  # Deterministic RNG seed for spawning/routing (keeps runs reproducible).
  random-seed: 20260604
  # Default signal phase durations (seconds).
  ns-green-seconds: 15.0
  ew-green-seconds: 15.0
  yellow-seconds: 3.0
  all-red-seconds: 2.0
  # Distance (m) before the stop line at which an emergency vehicle triggers preemption.
  emergency-trigger-m: 80.0

logging:
  level:
    com.trafficflow: INFO
