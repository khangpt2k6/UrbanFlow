# Copy to .env.local for local overrides, or set in the Vercel project's Environment Variables.
# SockJS endpoint of the deployed backend. Must be the https:// URL (not ws://) ending in /ws.
# Leave unset for local development - the app defaults to http://localhost:8080/ws.
VITE_WS_URL=https://urbanflow-backend.onrender.com/ws
